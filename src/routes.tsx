import {BrowserRouter, Route, Routes} from "react-router-dom";

const myRoutes = () => {
    return(
        <BrowserRouter>
            <Routes>
                <Route/>
            </Routes>
        </BrowserRouter>
    )
}

export default myRoutes;